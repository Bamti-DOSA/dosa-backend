server:
  port: 8080
  # [팀원 추가] 에러 발생 시 상세 내용을 보여줌 (디버깅용)
  error:
    include-message: never
    include-binding-errors: never
  # 한글 깨짐 방지
  servlet:
    encoding:
      force: true
      charset: UTF-8
      enabled: true

spring:
  application:
    name: dosa

  # --------------------------------------------------------
  # 🚨 DB 설정(datasource, jpa)은 우리가 사용하지 않으므로 제거함
  # --------------------------------------------------------

  # [S3 설정] 환경변수 사용
  cloud:
    aws:
      s3:
        bucket: "dosa-3d-models"
      credentials:
        access-key: ${AWS_ACCESS_KEY}
        secret-key: ${AWS_SECRET_KEY}
      region:
        static: ap-northeast-2

openai:
  api-key: ${OPENAI_API_KEY}
  model: gpt-5-mini
  api:
    url: https://api.openai.com/v1/chat/completions